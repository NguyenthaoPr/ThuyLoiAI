# Web Framework & Async Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Google GenAI SDK (Cơ chế Interactions API & File Search)
google-genai>=0.1.1

# Xử lý Upload File (Bắt buộc cho endpoint /upload với UploadFile)
python-multipart>=0.0.9

# Xử lý định dạng dữ liệu (Đã tích hợp trong FastAPI nhưng nên khai báo rõ)
pydantic>=2.6.0
Pillow
reportlab
